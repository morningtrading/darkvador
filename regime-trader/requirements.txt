# ─── PINNED VERSIONS for cross-platform reproducibility ─────────────────
# Loose version pins (>=) cause HMM EM to converge to different local
# optima across machines (numpy 2.4 vs 2.2, different BLAS back-ends,
# etc.). These exact pins + single-threaded BLAS (set in main.py top)
# make Windows and Linux backtests identical.
# Reference machine: Windows 11, Python 3.12.10.
# Regenerate: py -3.12 -m pip freeze > requirements-freeze.txt

# HMM & ML
hmmlearn==0.3.3
scikit-learn==1.8.0

# Broker / market data
alpaca-trade-api>=3.0.0
alpaca-py>=0.13.0

# Data processing
pandas==3.0.2
numpy==2.4.4
scipy==1.17.1

# Technical analysis
ta>=0.11.0

# Config & environment
pyyaml>=6.0
python-dotenv>=1.0.0

# Scheduling
schedule>=1.2.0

# WebSocket
websocket-client>=1.6.0

# Terminal dashboard
rich>=13.0.0

# Testing
pytest>=8.0.0
pytz>=2024.1
